# Mirrored/derived dataset artifacts (large binaries live in the GitHub Release, not the repo)
data/*.dta
data/*.dta.part*
data/*.pkl
data/*.csv
!data/ppml_*_results.csv
!data/rq_us_only_*.csv
data/*.log
data/swapfile
